function buildPlayerMouvement () {
    screen.registerBeforeRender(function () {

        if (!screen.isReady()) {
            return;
        }

        if (wPressed) {
            
            const 

            playerMesh.locallyTranslate()
        }

    });
}